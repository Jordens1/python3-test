标签：
	<br>	定义换行
	<button>	定义一个点击按钮
	<div>	定义文档中的节
	<head>	定义关于文档的信息
	<hr>	定义水平线
	<img>	定义图像
	<link>	定义文档与外部资源的关系
	<ul>	定义无序列表。
	<h1> to <h6>	定义 HTML 标题
	<p>	定义一个段落

点语法：
	
	
HTML转义：
	

结构标签：
	block:用来规划布局（首次出现），第二次出现就是填充以前的规划，第三次出现，默认覆盖，可以添加{{ block.super }}实现增量操作
	extends：继承，获取模板中的所有结构
	include（少用）：包含，将页面作为一部分，嵌入其他页面
	继承自父模板的，只能在父模板中填充，不能自己再重写

静态资源：
	创建静态资源文件夹
	settings注册:STATICFILES_DIRS={os.path.join(BASE_DIR, / /)}
	在模板中先加载:
			{% load static %}
			{% static 'XXX' %}   xxxx是相对路径
		在debug模式可以使用
		生产中需要自己独立处理

locals()   内置函数，可以将局部变量使用字典的方式进行打包，key是变量名，value是变量存储的值


(？<year>\d+)  关键字参数匹配  urls里

反向解析：使用namespace    就是用name代替路径，反向解析出来，要加变量就在html后面加










